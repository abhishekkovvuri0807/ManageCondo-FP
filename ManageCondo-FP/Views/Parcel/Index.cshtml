@model IEnumerable<ManageCondo.DomainModels.ParcelViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ParcelID)
        </th>
        <th>
            @Html.DisplayName("Email")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Courier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isIncoming)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfPeices)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayName("First Name")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ParcelID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Resident.User.Email)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Courier)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.isIncoming)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NumberOfPeices)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Description)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Resident.User.FirstName)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.ParcelID }) |
        @Html.ActionLink("Details", "Details", new { id = item.ParcelID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ParcelID })
    </td>
</tr>
}

</table>
